/**
 * config.js — Frontend feature flags for Chota Packet.
 *
 * Toggle UI features without touching component code.
 */

const CONFIG = {
  /** Show/hide the "Backend connected / offline" bar below the navbar. */
  SHOW_BACKEND_STATUS_BAR: true,
};

export default CONFIG;
